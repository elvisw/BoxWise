# Decision Log — SPEC-2fa-multi-method-login

## 2026-05-30: Initial spec creation

**Source:** `_bmad-output/implementation-artifacts/investigations/2fa-code-invalid-investigation.md`

**Verdict (Pass 1 — Coherence):** All 8 Spec Law rules pass. 5 capabilities each have intent + success pairs. Constraints are design-bending (backward compat, flags enum, API optional field). 5 non-goals explicitly bound scope. Success signal is demonstrable (DB check + login test). Lean prose throughout.

**Verdict (Pass 2 — Preservation):** All 7 Confirmed Findings from the investigation case file are covered. Finding 2 (TotpSecretKey cleared) → data-model.md fix. Finding 3 (single-value enum) → data-model.md schema change. Findings 4-5 (routing) → login-flow.md. Finding 6 (frontend) → login-flow.md frontend section. Finding 7 (fire-and-forget SMTP) → explicitly listed as Non-goal. No load-bearing claim lost.

**Design decisions made:**
1. `[Flags]` enum over separate bool fields — minimal schema change, backward compatible, INT column unchanged
2. Optional `Method` field in VerifyTwoFactorRequest over required field — backward compatible for old clients
3. Fallback routing for null Method — single-method users unaffected
4. Rename `TwoFactorMethod` → `ConfiguredMethods` — clarity that this is a set, not a selection

**Assumptions:** 3 assumptions recorded. All verifiable during implementation.

**Open Questions:** None. Investigation provided complete code-level evidence.

## 2026-05-30: Review-driven updates (round 2)

**Review source:** 3 parallel sub-agents (correctness, edge cases, completeness) — second round

**All 3 agents: ✅ Approved for implementation**

**Changes made in round 2:**

| # | Issue | Source | Resolution |
|---|-------|--------|------------|
| A | EmailForTwoFactor 为 null 时 ChallengeAsync 可能 NRE | 边缘案例审查 | 添加 null 检查 + 自动清除损坏的 Email 标志位 |
| B | VerifyAsync 未校验指定方法是否实际配置 | 边缘案例审查 | 每个 case 分支添加 HasFlag 前置检查，返回 "该方法未配置" |
| C | 重新发送可能双重发信 | 边缘案例审查 | send-challenge-code 端点改为返回新 token，前端不再二次调用 Challenge |
| D | SwitchMethodAsync 废弃影响未记录 | 边缘案例审查 | 添加 Known Limitation |
| E | Email 验证码在 TOTP 选择时仍发送 | 正确性 + 边缘案例审查 | 添加注释说明为有意权衡 + Known Limitation |
| F | ChallengeAsync 防御性检查返回格式未指定 | 正确性审查 | 指定返回 LoginResponse（非 TwoFactorChallengeResponse） |
| G | send-challenge-code 端点未在 API 变更中定义 | 正确性审查 | 新增 SendChallengeCodeAsync 端点改造 + SendChallengeCodeResponse DTO |

**Final verdict:** Spec ready for implementation. 5 CAPs, 8 Constraints, 6 Non-goals, 6 Known Limitations. All 14 investigation items covered.
