# Decision Log — SPEC-identity-scaffold-migration

**Date:** 2026-06-01
**Source:** `_bmad-output/planning-artifacts/research/technical-identity-scaffold-hybrid-migration-research-2026-05-31.md`（4 轮反面审核，34 个问题已修复）

## Self-Validation Verdict

**Pass 1 (Coherence):** 7 个 Capability 均有 intent+success（CAP-8 已删除）。CAP-1 intent 已从 HOW 重写为 WHAT。CAP-6 intent/success 已修正为描述完整用户旅程（Identity→Blazor WASM→WebAuthn）。C4 改为使用 `ISmtpConfigurationService` 接口（与项目现有 DI 约定一致）。C7 改为强制语言。7 个 Constraint 每个都排除真实的设计选项，7 个 Non-goal 明确边界。Success signal 可演示。

**Pass 2 (Preservation):** 调研报告中所有 load-bearing 主张均已落入 SPEC.md 或两个 companion。退役清单（`decommission-checklist.md`）与迁移路线图（`migration-phases.md`）承载了操作级细节。3 个 open questions 来自调研报告中的待验证项。

**Wrapper-only content:** 调研报告中的叙述性分析（技术栈比较、反面审核发现、社区踩坑记录）未进入 SPEC——这些是过程性内容，下游实施者不需要。

## Key Decisions

1. **IEmailSender 适配器用 `SmtpConfigurationService` + MimeKit 直接发送**，不委托 `EmailTwoFactorService.SendVerificationEmailAsync`（API 签名不兼容）
2. **Login.razor 部分退役**——标准登录/2FA 由 Identity 页面接管，通行密钥代码保留
3. **每个 Phase 独立 commit**——非一个巨大 commit，支持精确回滚
4. **Spec 格式选 express**——调研报告内容丰富，无需引导式填写
