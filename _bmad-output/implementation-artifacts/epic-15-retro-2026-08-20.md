---
epic: 15
date: 2026-08-20
verdict: accepted
criteria: profiled
headless: false
---

# Epic 15 Retrospective: 忘记密码功能

## Epic Summary

**目标:** 实现用户自助忘记密码重置流程（用户名输入 → 邮件发送重置链接 → 设新密码）。

**来源文档:** `_bmad-output/planning-artifacts/2026-07-02-forgot-password-design.md`（设计文档）+ `_bmad-output/implementation-artifacts/epic-15-forgot-password-plan.md`（实施计划，任务式而非标准 Story 格式）。

**Diff 范围:** `a0f68d1..cc773b5`（17 commits，含 1 个依赖 bump merge；11 个功能/文档/测试提交）

**Story 状态:** 15-1-forgot-password-reset = done（1/1 完成，pending_stories 为空）

**证据清单:**
- 设计文档（2026-07-02-forgot-password-design.md）：安全措施 14 项 + 数据流 + 涉及文件
- 实施计划（epic-15-forgot-password-plan.md）：8 个任务
- 代码：4 个新 Identity Razor 页面（ForgotPassword/Confirmation, ResetPassword/Confirmation）+ Program.cs + appsettings.json
- 测试：`ForgotPasswordE2ETests.cs` 395 行（11 个测试，WebApplicationFactory + 真实 SQLite）
- 手动测试指南：`epic-15-manual-test-guide.md`（7 组场景）
- 脚手架修改记录：`docs/identity-scaffold-modifications.md` 已更新
- 缺失：无 session logs（过程教训分析受限于代码/文档证据）

## Findings

### Aggregate Views

| # | View | 结论 | 来源 |
|---|------|------|------|
| A1 | Architecture delta | 无新依赖层、无架构变更。4 个页面完全遵循 Identity 脚手架模式（与 Epic 10/11 迁移后的模式一致），速率限制策略与 login 策略同构 | git_evidence.py files；Program.cs diff |
| A2 | God-class / size growth | 无。最大新增文件 ResetPassword.cshtml.cs 127 行，其余均 < 80 行 | git_evidence.py files |
| A3 | Duplication map | ForgotPasswordConfirmation / ResetPasswordConfirmation 两个纯提示页结构相同（各 11+14 行）——Identity 模板标准模式，接受 | git_evidence.py files |
| A4 | Pattern divergence | 未发现。中文化错误消息、`[AllowAnonymous]`、`[EnableRateLimiting]`、`Trim` 校验均与现有 Identity 页面约定一致 | 代码审查 |
| A5 | Spec reconciliation | 设计文档 14 项安全措施全部落地：统一响应防枚举、token 1h 过期、IP 速率限制 60s/1 次、未确认邮箱不发送、一次性 token、双重 Base64Url 编码、绝对 URL、重置后不自动登录、重置通知邮件、4 页面 AllowAnonymous。设计文档"还未确定的优化"部分按定义未实现（非承诺项） | 设计文档 125-139 行 ↔ 实现代码 |

### Diff-scope Review（内联执行，范围收窄为新增代码全量审查）

| # | 发现 | 来源 | 处置 |
|---|------|------|------|
| R1 | **无效 code 导致 500**：`ResetPassword.cshtml.cs:74` 的 `WebEncoders.Base64UrlDecode(code)` 对非法输入（如 `code=!!!`）抛 `FormatException` → 500。继承自 ASP.NET Identity 原版模板（模板本身无 try-catch），项目其他页面（ConfirmEmail 等）同为该模式。低风险：匿名页面、无敏感泄露 | ResetPassword.cshtml.cs:74；模板对照 | **accept as-is**（记录以停止后续 re-flag） |
| R2 | 防枚举正确：不存在用户/未确认邮箱/成功均重定向到同一 Confirmation 页，不发邮件（R1 外无信息泄露路径） | ForgotPassword.cshtml.cs:47-70 | 通过 |
| R3 | OnPost 用户不存在 → 重定向 Confirmation（不泄露账户存在性） | ResetPassword.cshtml.cs:81-85 | 通过 |
| R4 | 速率限制仅作用于 POST（`[EnableRateLimiting]` 在 OnPostAsync 上），GET 不受限——正确，GET 无副作用 | ForgotPassword.cshtml.cs:42 | 通过 |
| R5 | appsettings 新增配置键与读取键一致（`RateLimit:ForgotPasswordPermitLimit`/`ForgotPasswordWindowSeconds`），窗口单位 Seconds 与代码 `TimeSpan.FromSeconds` 匹配 | appsettings.json；Program.cs:214-218 | 通过 |
| R6 | MaskEmail 对空邮箱/无 @/短用户名（≤2 字符）均有分支处理 | ResetPassword.cshtml.cs:112-125 | 通过 |

### 测试与行为验证

**E2E 测试（ForgotPasswordE2ETests.cs，11 个）** 使用 WebApplicationFactory + 真实 SQLite + 模拟 IEmailSender，实测 HTTP 层与 PageModel 层：防枚举（不存在用户）、未确认邮箱不发邮件、空用户名验证、缺 code 400、无效用户重定向、密码长度/匹配验证、无效 token。**E2E 测试即行为验证**——未单独运行完整应用（无 SMTP 环境），此为收窄项。

**未自动化场景**（合理，依赖时间/真实邮件）：速率限制 429（指南 T4）、token 过期（T5）、token 复用（T6）——由手动测试指南 7 组场景覆盖，尚未执行记录。

## Previous-retro Follow-through

- Epic 14 的 retro 文档**不存在**（sprint-status.yaml 中 `epic-14-retrospective: done`，但 `epic-14-retro-*.md` 文件缺失）——无法检查 follow-through。记录此缺口。
- 顺带观察（非正式）：Epic 13 action item "建立 WebApplicationFactory 集成测试基础设施" 已在 Epic 15 落地（ForgotPasswordE2ETests.cs 即成果）；"Lazy<T> 缓存 TTL" 仍未处理（AiService.cs:15 仍为永久 Lazy 缓存）。

## Action Items

1. **恢复标准 Story 文件格式** — Epic 15 采用任务式计划文件（`epic-15-forgot-password-plan.md`，`## 任务 N` 标题），导致 `sprint_plan.py` 无法解析、sprint-status 需手动补录。后续 Epic 应创建 `{N}-{M}-*.md` 标准 Story 文件（`# Story N.M: 标题` + `### Story {N}.{M} 前置依赖` 格式）。
   Owner: Elvis | 类别: 过程 | 状态: open
2. **执行手动测试指南** — `epic-15-manual-test-guide.md` 的 7 组场景（入口可见性、正常流程、防枚举、速率限制、token 过期、token 复用、无效链接）尚未执行。建议在部署或正式验收前执行并记录结果。
   Owner: Elvis | 类别: 验证 | 状态: open

## Acceptance Verdict

**verdict: accepted**（criteria: profiled）

- 计划文件未声明正式验收标准（任务 8 为构建/测试/手动验证清单），从设计文档安全措施 + 交付物 profiling：
- 设计文档 14 项安全措施全部实现（A5），无 blocking findings
- 全部 story 完成（pending_stories 为空）
- 唯一的 defer 级发现（R1 无效 code 500）继承自模板、低风险，记录为 accepted 而非 open item

## Open Questions

- 无重大未决问题。R1 若未来升级 Identity 模板或统一处理 base64 解码异常可一并修复（不阻塞本 epic 验收）。

## 补充说明

- Epic 15 无标准 story 文件 → sprint-status 中 15-1 条目为本次手动补录（key 为 `15-1-forgot-password-reset`，非解析器生成）。
- Epic 14 retro 文档缺失：建议后续补齐或在未来 retro 中处理（见 Action Item 1 的过程教训）。
