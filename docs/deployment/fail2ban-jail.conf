# BoxWise fail2ban jail configuration
# 适用于 Debian VPS 部署
# 安装: cp boxwise.conf /etc/fail2ban/jail.d/
# 重载: sudo systemctl reload fail2ban

[boxwise-auth]
enabled = true
port = http,https
filter = boxwise
logpath = /var/log/boxwise/*.log
maxretry = 5
findtime = 600
bantime = 1800
